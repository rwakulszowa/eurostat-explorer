---
pagination:
  data: datasets
  size: 1
  alias: dataset
  addAllPagesToCollections: true
tags: datasetPages

permalink: "{{ dataset.id }}"

layout: layout.njk
---

<p>
  Dataset: {{ dataset.id }}
  {{ dataset.description }}
</p>
<script type="module">
  const resp = await fetch("./ {{ dataset.id }}.json");
  const obj = await resp.json();
  console.log(obj);
</script>